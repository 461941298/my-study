什么要使用代理？
    为了增强原有的功能。

如何增强？
    在原有的功能前后添加逻辑。

如何实现代理？
    如果要增强功能F,那么F就可以视为一个功能接口fun；
    原来拥有F功能的对象就是被代理对象target，在代理模式中target要实现fun；
    新的代理对象proxy要持有一个target实例，并实现fun接口，在实现的方法中调用target的F，这里可以在调用F的前后添加逻辑。
    如此调用proxy的F功能，相当于调用了target的增加的F功能。

